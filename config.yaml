# Polaris Gateway 配置文件
server:
  host: "127.0.0.1"     # 默认只允许本地访问
  port: 28080            # 监听端口
  max_concurrency: 50   # 全局最大并发请求数 (新增)
  read_timeout: 15      # 分钟
  write_timeout: 15     # 分钟

database:
  primary: "polaris.db"
  l2: "polaris_l2.db"

storage:
  vfs_path: "./data/vfs"